services:
  stockmaster:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stockmaster-ui
    env_file:
      - .env
    volumes:
      - .:/workspace
    ports:
      - "8501:8501"
    command: streamlit run app/ui/Home.py --server.address=0.0.0.0 --server.port=8501
